<% if @user.new_record? %>
<h1>Editing user</h1>
<% else %>
<h1>Change password for user <%= @user.username %></h1>
<% end %>

<%= render "form", user: @user %>

<br>

<div>
  <%= link_to "Show this user", @user %> |
  <%= link_to "Back to users", users_path %>
</div>
